<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= pkg.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-body">
                <h2 class="card-title"><%= pkg.name %></h2>
                <p class="card-text"><strong>Destino:</strong> <%= pkg.destination %></p>
                <p class="card-text"><strong>Data de Partida:</strong> <%= pkg.departure %></p>
                <p class="card-text"><strong>Duração:</strong> <%= pkg.duration %></p>
                <p class="card-text"><strong>Preço:</strong> R$ <span id="price"><%= pkg.price.toFixed(2) %></span></p>
                <p class="card-text"><strong>Lugares Disponíveis:</strong> <%= pkg.availableSpots %></p>
                <div class="mb-3">
                    <label for="quantity" class="form-label">Quantidade:</label>
                    <input type="number" id="quantity" class="form-control" min="1" max="<%= pkg.availableSpots %>" value="1">
                </div>
                <p><strong>Total:</strong> R$ <span id="total"><%= pkg.price.toFixed(2) %></span></p>
                <a href="/" class="btn btn-secondary">Voltar</a>
            </div>
        </div>
    </div>
    <script src="/js/detail.js"></script>
</body>
</html>