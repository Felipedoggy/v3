<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= pkg.destino %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-body">
                <h2 class="card-title"><%= pkg.destino %></h2>
                <p class="card-text"><strong>Descrição:</strong> <%= pkg.descricao %></p>
                <p class="card-text"><strong>Data de Partida:</strong> <%= pkg.data_partida.toLocaleDateString('pt-BR') %></p>
                <p class="card-text"><strong>Duração:</strong> <%= pkg.duracao %> dias</p>
                <p class="card-text"><strong>Preço Unitário:</strong> R$ <span id="price"><%= parseFloat(pkg.preco).toFixed(2) %></span></p>

                <!-- Campo de Quantidade -->
                <div class="mb-3">
                    <label for="quantity" class="form-label">Quantidade:</label>
                    <input type="number" id="quantity" class="form-control" min="1" value="1">
                </div>

                <!-- Total -->
                <p><strong>Total:</strong> R$ <span id="total"><%= parseFloat(pkg.preco).toFixed(2) %></span></p>

                <!-- Botão de Comprar -->
                <button id="buyButton" class="btn btn-primary mt-3">Comprar</button>

                <!-- Botão de Voltar -->
                <a href="/" class="btn btn-secondary mt-2">Voltar</a>
            </div>
        </div>
    </div>

    <!-- Script JavaScript -->
    <script src="/js/detail.js"></script>
</body>
</html>